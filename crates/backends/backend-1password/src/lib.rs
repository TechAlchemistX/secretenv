//! 1Password backend for SecretEnv.
//!
//! Wraps the `op` CLI to read and write items stored in 1Password
//! vaults. Supports multiple `1password-*` instances via the
//! `op_account` config field.
//!
//! This scaffolding crate is intentionally empty. Implementation lands in Phase 6.
#![forbid(unsafe_code)]
