# Project manifest — committed to the repo. Zero infrastructure info.

[secrets]
API_KEY = { from = "secretenv://api-key" }
DB_URL  = { from = "secretenv://db-url"  }

# Static defaults get injected as-is (no backend involved).
LOG_LEVEL = { default = "info" }
