# v0.17 OTel smoke fixture — project-side manifest. Resolves a single
# alias from the local-only registry so the smoke can assert span
# emission without any cloud-backend dependency.
[secrets]
OTEL_SENTINEL = { from = "secretenv://otel_sentinel" }
